#include "main.h"

/**
 * main - write a code that print a lowercase alphabet
 * Return: Always 0
 */

void print_alphabet(void)
{
	int lw;
	for (lw = 'a'; lw <= 'z'; lw++)
	_putchar(lw);
	_putchar('\n');
}
